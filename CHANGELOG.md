# 更新日志

本文件记录各版本**用户可见的变化**。更早版本的完整说明与安装包见
<https://github.com/ANG-LD/screenshot-rs/releases>。

## [0.1.4] - 2026-09-24

### 修复

- **遮罩出现后整屏偏色（红蓝互换）**：把捕获帧交给 GPU 图集前的 RGBA→BGRA 换道被做了
  两遍，两次对调等于没换——覆盖层里的画面因此 R/B 互换（红色变蓝、蓝色变橙）。
  现在恰好换一次，并加了回归测试钉住。
- **GNOME 顶栏那条（屏幕最上方 32px）里无法起框**：那一条的指针事件被 GNOME Shell
  截走，改为覆盖层显示期间用 16ms 轮询补齐按下/移动/抬起——从最顶部往下拉、从下往上
  拉到顶都能正常框选。
- **顶栏那条里十字与坐标徽章不跟手**：轮询接管的整段拖拽漏刷了光标读数，导致它们停在
  起框那一刻；现在全程跟随指针。
- **覆盖窗口盖不住 GNOME 顶栏 / 启动瞬间黑屏**：覆盖窗口回到 `PopUp` 窗口类型，
  自绘光标在顶栏那条里钉在其下沿显示（那一条的观感交给系统光标）。
- 文本框内边距随字号缩放、文本光标随字号加宽，修掉小字号裁字。
- 同一版本重新打包时按文件时间刷新用户目录里的副本（自动更新不再漏更新）。
- Linux 构建下不再出现 Windows 专属代码的 unused 告警。

### 新增

- 框选时的十字光标；马赛克笔刷改为「指针即圆盘」。
- 马赛克「一键模糊」二级弹窗（框选模糊 / 内容占比达 80% 还原）。
- 马赛克一笔遮挡 + 实时预览，且只对图像内容打码。
- 单实例锁：同一会话不再允许开多个实例。

### 性能

- 覆盖层每帧的帧图像构造合并了内存拷贝与通道换道（1080p 整帧 1.36ms → 0.88ms）；
  马赛克渲染路径改为先查缓存。
- OCR / 翻译链路去掉三处重复的大缓冲开销：选区像素不再多拷一整份（原来在
  `clipped.pixels.clone()` 处再拷一次整个选区 RGBA）；RGBA→RGB 转换由逐字节 `push`
  改为定长缓冲 + 分块写（release 实测 2.2~2.4×，1200×700 选区 2.37ms → 1.08ms）；
  识别引擎直接 move 已建好的图像，不再 `to_vec()` 重建一张（默认纯 CPU 配置下零拷贝）。
